import {BaseIdentity} from './BaseIdentities';


/**
 * PageView class identity
 */
export class PageView extends BaseIdentity {
    /**
     * Resets identity to a new one
     */
    init() {
        this.resetToNew();
    }
}
